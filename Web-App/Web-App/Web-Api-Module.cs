using Autofac;
using Microsoft.AspNetCore.Mvc;

namespace Web_App
{
    public class Web_ApiModule : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            
            base.Load(builder);
        }
    }
}
